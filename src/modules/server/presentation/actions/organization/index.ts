/**
 * Organization server actions barrel.
 *
 * Layer: presentation / actions / organization
 *
 * Single import point for all Organization server actions.
 * Import from this folder barrel — never from organization.actions directly.
 */

export * from "./organization.actions";
