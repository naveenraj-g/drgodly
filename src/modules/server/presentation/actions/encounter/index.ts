/**
 * Encounter server actions barrel.
 *
 * Layer: presentation / actions / encounter
 *
 * Re-exports all encounter server actions.
 * Client code imports from this barrel — never from core.actions.ts directly.
 */

export * from "./core.actions";
